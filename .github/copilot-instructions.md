Read the root `AGENTS.md` first. Follow the repository-specific documents it names.

Use `PLAN -> CHANGESET -> VERIFY -> VERDICT`, keep changes small, use repository-native CI, and group related GitHub edits into coherent commits. Preserve unrelated work and follow the repository's existing review and merge rules.
