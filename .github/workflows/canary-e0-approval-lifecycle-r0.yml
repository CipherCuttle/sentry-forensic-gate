name: canary-e0-approval-lifecycle-r0

on:
  push:
    branches:
      - agent/canary-e0-approval-lifecycle-r0
    paths:
      - 'src/canary/approval.ts'
      - 'src/canary/approvalStore.ts'
      - 'scripts/canary-e0-approval-lifecycle-r0-check.mjs'
      - '.github/workflows/canary-e0-approval-lifecycle-r0.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qualify-approval:
    if: github.ref == 'refs/heads/agent/canary-e0-approval-lifecycle-r0'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      INK_RPC_URL: https://rpc-gel.inkonchain.com
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: corepack enable
      - run: pnpm install --no-frozen-lockfile --lockfile=false
      - run: pnpm build
      - name: Preserve narrow wallet-authority boundary
        run: node scripts/canary-boundary-check.mjs
      - name: Assert qualification has no wallet secret
        shell: bash
        run: |
          set -euo pipefail
          test -z "${CANARY_PRIVATE_KEY:-}"
          echo 'CANARY_E0_APPROVAL_NO_KEY_BOUNDARY=PASS'
      - name: Qualify exact-bounded approval lifecycle
        shell: bash
        run: |
          set -euo pipefail
          node scripts/canary-e0-approval-lifecycle-r0-check.mjs | tee canary-e0-approval-lifecycle-r0.json
      - name: Upload qualification evidence
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: canary-e0-approval-lifecycle-r0-evidence
          path: canary-e0-approval-lifecycle-r0.json
          if-no-files-found: error
