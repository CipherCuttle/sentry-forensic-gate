# PONS S1 dual-RPC rehearsal — one bounded hostile self-review

One internal adversarial review of the PR-only engineering diagnostic;
not an external independent RPC/security/science assessment.

- High fixed: first real Action proved two-endpoint factory access but
  captured zero native launches in a 12-block sample. Replace with 256
  contiguous blocks, strict per-chunk and total full-census parity.
  Still non-enrolling, never extrapolate to all launches.
- High fixed: recent RPC service was misread as archive capability.
  Add historical pinned block/code and historical contract eth_call
  parity. This samples archive access only; backend independence remains
  unqualified and hardcoded false.
- High fixed: equality of five arbitrary quote legs was insufficient.
  Require exact ordered frozen five USD notionals on both sources and
  adversarial duplicate-ladder rejection.
- High fixed: reorg check re-read launch but omitted decision block.
  Independently reacquire launch AND launch+2 decision block/hash after
  inclusion+12.
- Open Medium: rate-limits and absent native events remain explicit
  PROVIDER_FAIL or NO_NATIVE_IN_BOUNDED_WINDOW. A single event is not
  statistical evidence.
- Open Medium: 150-second cooperative budget is checked before calls.
  Individual viem calls time out at 12s, Actions has 6-minute hard cap.
- Open High for ACTIVE SCIENCE: provider backend independence, complete
  cursor census, pre-outcome externally authenticated source and batch
  publication, full route/net cost and independent power still UNPROVEN.

Targeted rereview after exact-head CI and real network results only.
No merge, trading, S1 activation, release creation or money authority.

## Targeted follow-up: unqualified historical service

The first expanded live test failed at the combined historical probe with
sanitized RPC_UNAVAILABLE, hiding which public endpoint or method failed.
Split read-only historical block, code and eth_call by provider with finite
error-stage records. Continue a separate bounded RECENT quote diagnostic
without promoting it as an archive-qualified observation. Pure tests require
HISTORIC_SAMPLE_REQUIRED for the stronger technical verdict and reject
claiming an unqualified archive as qualified. One exact-head CI and real
network rerun is the targeted closure check; an external provider failure
remains a documented blocker rather than triggering more review loops.
